export class exerciseModel {
    constructor(public kind: string, public link: string,
             public name: string, public rep: string,
             public score: number, public series: number) {}
  }
  